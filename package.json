{
  "name": "transferverified",
  "private": true,
  "type": "module",
  "description": "Serverless function dependencies only. The frontend has no build step.",
  "dependencies": {
    "@clerk/backend": "^2.6.0"
  }
}
